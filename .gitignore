# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
#.pws  # this contains lab passwords anyhow, if anything more then un comment this.
#*.pws
*.log

# Dependency directories (remove the comment below to include it)
# vendor/
.DS_Store
.idea/
.vscode/

# Just demo environment, so never mind excluding these
#.pws
#.env

compose/data/
docker/data/

old/
shadowtraffic/
